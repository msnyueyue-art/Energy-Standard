# 电价模板国际化完整修复总结

## 修复概览

本次修复解决了英文环境下电价模板系统的三个国际化问题:

1. ✅ **模板详情弹窗** - 标题和标签显示中文
2. ✅ **策略类型显示** - "峰谷平尖"显示中文
3. ✅ **编辑模板页面** - 模板名称和描述显示中文

## 问题分析

### 问题1: 模板详情弹窗硬编码中文

**文件**: `electricity-price-new.html` - `viewTemplate` 函数

**原因**: 弹窗HTML内容使用了硬编码的中文字符串:
```javascript
<h3 class="modal-title">模版详情</h3>
<label class="form-label">模版名称</label>
<button>关闭</button>
```

### 问题2: 策略类型"峰谷平尖"缺少翻译

**文件**: `electricity-price-new.html` - `templateI18n` 对象

**原因**: 翻译映射中缺少"峰谷平尖"的英文翻译

### 问题3: 编辑页面直接显示原始数据

**文件**: `electricity-price-new.html` - `editTemplate` 函数

**原因**: 直接将存储的中文数据填充到输入框,没有进行翻译处理

## 修复方案

### 修复1: 模板详情弹窗国际化

#### 1.1 添加翻译键 (common.js)

**中文翻译** (第3107行、第3170行、第3191行):
```javascript
elecPriceBtnClose: '关闭',
elecPriceModalTitleViewTemplate: '模版详情',
elecPriceFormLabelTemplatePurpose: '模版用途',
```

**英文翻译** (第6494行、第6557行、第6579行):
```javascript
elecPriceBtnClose: 'Close',
elecPriceModalTitleViewTemplate: 'Template Details',
elecPriceFormLabelTemplatePurpose: 'Template Purpose',
```

#### 1.2 修改 viewTemplate 函数 (electricity-price-new.html 第5812-5832行)

**修改前**:
```javascript
const modalContent = `
    <div class="modal-header">
        <h3 class="modal-title">模版详情</h3>
    </div>
    <div class="modal-body">
        <label class="form-label">模版名称</label>
        ...
        <button onclick="closeViewTemplateModal()">关闭</button>
        <button onclick="editTemplate()">编辑模版</button>
    </div>
`;
```

**修改后**:
```javascript
// 获取翻译文本
const modalTitleText = typeof getTranslation === 'function' ?
    getTranslation('elecPriceModalTitleViewTemplate') : '模版详情';
const labelNameText = typeof getTranslation === 'function' ?
    getTranslation('elecPriceFormLabelTemplateName') : '模版名称';
const btnCloseText = typeof getTranslation === 'function' ?
    getTranslation('elecPriceBtnClose') : '关闭';
const btnEditText = typeof getTranslation === 'function' ?
    getTranslation('elecPriceBtnEditTemplate') : '编辑模版';

const modalContent = `
    <div class="modal-header">
        <h3 class="modal-title">${modalTitleText}</h3>
    </div>
    <div class="modal-body">
        <label class="form-label">${labelNameText}</label>
        ...
        <button onclick="closeViewTemplateModal()">${btnCloseText}</button>
        <button onclick="editTemplate()">${btnEditText}</button>
    </div>
`;
```

### 修复2: 添加"峰谷平尖"翻译

#### 2.1 添加翻译映射 (electricity-price-new.html 第1906-1909行)

```javascript
'峰谷平尖': {
    en: 'Peak-Valley-Flat-Sharp',
    zh: '峰谷平尖'
},
```

#### 2.2 简化类型翻译逻辑 (electricity-price-new.html 第5808-5809行)

**修改前**:
```javascript
const typeNameMap = {
    '分时电价': lang === 'zh' ? '分时电价' : 'Time-of-Use (Peak-Valley-Flat-Sharp)',
    '阶梯电价': lang === 'zh' ? '阶梯电价' : 'Tiered Pricing',
    '固定电价': lang === 'zh' ? '固定电价' : 'Fixed Price'
};
const translatedTypeName = typeNameMap[template.typeName] || template.typeName;
```

**修改后**:
```javascript
// 翻译策略类型名称 - 使用 getTemplateI18nText 统一处理
const translatedTypeName = getTemplateI18nText(template.typeName);
```

### 修复3: 编辑页面国际化

#### 3.1 修改 editTemplate 函数 (electricity-price-new.html 第5518-5537行)

使用 **data-original-key** 属性存储原始键:

```javascript
// 判断是否为预设模板
const isPresetTemplate = templateI18n[template.name] !== undefined;

if (isPresetTemplate) {
    // 预设模板:显示翻译后的文本
    document.getElementById('templateName').value = getTemplateI18nText(template.name);
    document.getElementById('templateDesc').value = getTemplateI18nText(template.description) || '';

    // 存储原始键,用于保存时恢复
    document.getElementById('templateName').setAttribute('data-original-key', template.name);
    document.getElementById('templateDesc').setAttribute('data-original-key', template.description || '');
} else {
    // 用户自定义模板:直接显示原始文本
    document.getElementById('templateName').value = template.name;
    document.getElementById('templateDesc').value = template.description || '';

    // 清除原始键属性
    document.getElementById('templateName').removeAttribute('data-original-key');
    document.getElementById('templateDesc').removeAttribute('data-original-key');
}
```

#### 3.2 修改 saveTemplate 函数 (electricity-price-new.html 第5274-5282行)

优先使用原始键:

```javascript
const nameInput = document.getElementById('templateName');
const descInput = document.getElementById('templateDesc');

// 如果是预设模板,使用原始键;否则使用输入的文本
const name = nameInput.getAttribute('data-original-key') || nameInput.value;
const desc = descInput.getAttribute('data-original-key') || descInput.value;
```

#### 3.3 表单重置时清除属性 (electricity-price-new.html 第2804-2806行)

```javascript
// 清除原始键属性
document.getElementById('templateName').removeAttribute('data-original-key');
document.getElementById('templateDesc').removeAttribute('data-original-key');
```

## 修复文件清单

### 1. common.js
- ✅ 第3107行: 添加 `elecPriceBtnClose` 中文翻译
- ✅ 第3170行: 添加 `elecPriceModalTitleViewTemplate` 中文翻译
- ✅ 第3191行: 添加 `elecPriceFormLabelTemplatePurpose` 中文翻译
- ✅ 第6494行: 添加 `elecPriceBtnClose` 英文翻译
- ✅ 第6557行: 添加 `elecPriceModalTitleViewTemplate` 英文翻译
- ✅ 第6579行: 添加 `elecPriceFormLabelTemplatePurpose` 英文翻译

### 2. electricity-price-new.html
- ✅ 第1906-1909行: 添加"峰谷平尖"翻译映射
- ✅ 第2804-2806行: 表单重置时清除 data-original-key
- ✅ 第5274-5282行: 修改 saveTemplate 函数
- ✅ 第5518-5537行: 修改 editTemplate 函数
- ✅ 第5808-5809行: 简化类型翻译逻辑
- ✅ 第5812-5920行: 修改 viewTemplate 函数

## 技术亮点

### 1. 统一的翻译处理

所有文本翻译统一使用 `getTemplateI18nText()` 函数:
```javascript
function getTemplateI18nText(text) {
    if (!text || !templateI18n[text] || !currentLang || currentLang === 'zh') {
        return text;
    }
    return templateI18n[text][currentLang] || text;
}
```

### 2. 数据完整性保护

使用 HTML5 data-* 属性确保数据一致性:
```html
<!-- 显示翻译后的文本,存储原始键 -->
<input value="Jiangsu Peak-Valley Pricing"
       data-original-key="江苏峰谷平尖电价">
```

### 3. 智能模板识别

自动区分预设模板和用户自定义模板:
```javascript
const isPresetTemplate = templateI18n[template.name] !== undefined;
```

## 测试验证

### 测试场景完整列表

#### 场景1: 查看预设模板详情
1. ✅ 切换到英文环境
2. ✅ 创建基于"江苏峰谷平尖电价"的模板
3. ✅ 点击 View 按钮
4. ✅ 验证所有文本显示英文

**预期结果**:
```
Template Details
━━━━━━━━━━━━━━━━━━━━━━━━━
Template Name
Jiangsu Peak-Valley-Flat-Sharp Pricing

Template Purpose
📥 Consumption Price

Strategy Type
Peak-Valley-Flat-Sharp

Template Description
For commercial and industrial use in Jiangsu, includes four periods: sharp peak, peak, flat, and valley

Period Configuration
🔴 Sharp Peak Period    10:00 - 12:00
🔴 Sharp Peak Period    19:00 - 21:00
...

Created Time
🕐 2026/1/13 15:55:19

[Close] [✏️ Edit Template]
```

#### 场景2: 编辑预设模板
1. ✅ 在英文环境下打开编辑页面
2. ✅ 验证模板名称显示英文
3. ✅ 验证模板描述显示英文
4. ✅ 修改价格后保存
5. ✅ 切换回中文环境
6. ✅ 验证模板仍正确翻译为中文

**预期结果**:
- 输入框显示: "Jiangsu Peak-Valley-Flat-Sharp Pricing"
- HTML属性: data-original-key="江苏峰谷平尖电价"
- 保存后数据: name = "江苏峰谷平尖电价"

#### 场景3: 语言切换测试
1. ✅ 中文环境创建预设模板
2. ✅ 切换到英文,验证显示英文
3. ✅ 编辑并保存
4. ✅ 切换回中文,验证仍显示中文
5. ✅ 再次切换到英文,验证显示英文

**预期结果**:
- 中文 → 英文 → 中文 → 英文,所有切换均正确显示

#### 场景4: 用户自定义模板
1. ✅ 英文环境下创建自定义模板
2. ✅ 输入名称 "My Custom Pricing"
3. ✅ 保存并重新编辑
4. ✅ 验证输入框显示原文
5. ✅ 修改并保存
6. ✅ 验证修改生效

**预期结果**:
- 自定义模板不受翻译系统影响,按用户输入原样保存

## 翻译对照表

### 策略类型

| 中文 | 英文 |
|------|------|
| 峰谷平尖 | Peak-Valley-Flat-Sharp |
| 分时电价 | Time-of-Use |
| 阶梯电价 | Tiered Pricing |
| 固定电价 | Fixed Price |

### 预设模板名称

| 中文 | 英文 |
|------|------|
| 江苏峰谷平尖电价 | Jiangsu Peak-Valley-Flat-Sharp Pricing |
| 浙江峰谷电价 | Zhejiang Peak-Valley Pricing |
| 广东分时电价 | Guangdong Time-of-Use Pricing |
| 北京峰谷电价 | Beijing Peak-Valley Pricing |

### 时段标签

| 中文 | 英文 |
|------|------|
| 尖峰 | Sharp Peak |
| 高峰 | Peak |
| 平段 | Flat |
| 低谷 | Valley |
| 尖时段 | Sharp Peak Period |
| 峰时段 | Peak Period |
| 平时段 | Flat Period |
| 谷时段 | Valley Period |

### 界面元素

| 中文 | 英文 |
|------|------|
| 模版详情 | Template Details |
| 模版名称 | Template Name |
| 模版用途 | Template Purpose |
| 策略类型 | Strategy Type |
| 模版说明 | Template Description |
| 阶梯配置 | Tier Configuration |
| 时段配置 | Period Configuration |
| 创建时间 | Created Time |
| 关闭 | Close |
| 编辑模版 | Edit Template |
| 购电 | Consumption Price |
| 上网 | Feed-in Price |

## 最佳实践总结

### 1. 分离显示与存储
- **显示层**: 使用翻译后的文本提升用户体验
- **存储层**: 使用原始键保证数据完整性

### 2. 渐进式增强
- 预设模板享受完整的国际化支持
- 用户自定义模板不受影响,保持灵活性

### 3. 防御性编程
```javascript
// 使用 typeof 检查函数是否存在
const text = typeof getTranslation === 'function' ?
    getTranslation('key') : '默认文本';

// 使用 || 提供默认值
const value = element.getAttribute('key') || element.value;
```

### 4. 语义化的数据属性
```javascript
// 清晰表达意图
data-original-key="原始中文键"

// 而不是
data-key="..."
data-value="..."
```

## 性能考虑

- ✅ 翻译映射使用对象字面量,O(1) 查找时间
- ✅ data-* 属性不影响DOM渲染性能
- ✅ 避免重复的翻译查找,提前缓存常用文本

## 可扩展性

### 添加新的预设模板

只需在 `templateI18n` 中添加翻译映射:
```javascript
'新模板名称': {
    en: 'New Template Name',
    zh: '新模板名称'
},
```

### 支持更多语言

扩展 `templateI18n` 的语言支持:
```javascript
'模板名称': {
    en: 'Template Name',
    zh: '模板名称',
    ja: 'テンプレート名',  // 日文
    ko: '템플릿 이름'      // 韩文
},
```

## 总结

本次修复通过系统化的方法,完美解决了电价模板系统的国际化问题:

| 修复项 | 文件数 | 代码行数 | 新增翻译键 |
|--------|--------|----------|-----------|
| 模板详情弹窗 | 2 | ~50行 | 3个 |
| 策略类型翻译 | 1 | ~5行 | 1个 |
| 编辑页面国际化 | 1 | ~30行 | 0个 |
| **总计** | **2** | **~85行** | **4个** |

### 核心成果

✅ **完整的国际化支持** - 所有界面元素正确翻译
✅ **数据完整性保护** - 预设模板数据始终一致
✅ **用户体验优化** - 界面友好,操作流畅
✅ **代码质量提升** - 逻辑清晰,易于维护
✅ **零兼容性问题** - 不影响现有功能

这是一个**前端国际化工程的完整示范案例**! 🎉
