# 字段设置对话框国际化修复

## 修复时间
2026-01-12 晚上(最新)

## 问题描述

用户报告:在英文环境下,点击"自定义"按钮后打开的字段设置对话框中,所有字段名称和章节标题仍然显示中文。

### 截图中标记的问题:

**实时数据标签页:**
- ❌ 章节标题: "策略调度参数"、"核心运行参数"、"运行统计"、"其他参数"
- ❌ 字段标签: "当前策略"、"调度指令"、"目标SOC"、"最大功率"、"SOC"、"SOH"、"温度"、"充放电功率"、"电池电流"、"电池电压"、"今日充电量"、"今日放电量"、"累计充电量"、"累计放电量"、"循环次数"

**历史数据标签页:**
- ❌ 章节标题: "图表分析"
- ❌ 字段标签: "功率与SOC综合分析"、"收益分析"

**EMS设备(同样问题):**
- ❌ 章节标题: "CPU状态"、"内存状态"、"磁盘状态"、"开发板温度"、"4G网络状态"、"WiFi状态"、"系统信息"
- ❌ 字段标签: "使用率"、"温度"、"占用率"、"空间使用率"、"当前温度"、"信号强度"、"SIM卡状态"、"网络接口"、"系统运行时长"、"当前系统时间"、"当前硬件时间"

## 根本原因

字段设置对话框通过 `renderSettingsContent` 函数动态生成内容,该函数直接使用 `dataFieldsConfig` 中的中文字符串,没有进行国际化翻译:

1. **章节标题**: `sectionTitle.textContent = section;` - 直接使用中文章节名
2. **字段标签**: `label.textContent = config[section][field].label;` - 直接使用中文label
3. **历史数据章节**: `sectionTitle.textContent = category;` - 直接使用中文类别名
4. **历史数据字段**: `label.textContent = categoryFields[field].label;` - 直接使用中文label

## 修复内容

### 1. 修复 renderSettingsContent 函数 (data.html)

**实时数据部分章节标题 (第2509行):**
```javascript
// 修复前
sectionTitle.textContent = section;

// 修复后
sectionTitle.textContent = translateLabel(section);
```

**实时数据部分字段标签 (第2518行):**
```javascript
// 修复前
label.textContent = config[section][field].label;

// 修复后
label.textContent = translateLabel(config[section][field].label);
```

**历史数据部分章节标题 (第2565行):**
```javascript
// 修复前
sectionTitle.textContent = category;

// 修复后
sectionTitle.textContent = translateLabel(category);
```

**历史数据部分字段标签 (第2575行):**
```javascript
// 修复前
label.textContent = categoryFields[field].label;

// 修复后
label.textContent = translateLabel(categoryFields[field].label);
```

**错误提示信息 (第2497行):**
```javascript
// 修复前
settingsContent.innerHTML = '<p style="color: rgba(255,255,255,0.5); text-align: center;">该组件暂无可配置项</p>';

// 修复后
settingsContent.innerHTML = '<p style="color: rgba(255,255,255,0.5); text-align: center;">' + translateLabel('该组件暂无可配置项') + '</p>';
```

### 2. 扩展 translateLabel 函数 (data.html 第4093-4204行)

**新增章节标题翻译:**
```javascript
// 章节标题
'策略调度参数': 'Strategy Parameters',
'核心运行参数': 'Core Operation Parameters',
'运行统计': 'Operation Statistics',
'其他参数': 'Other Parameters',
'CPU状态': 'CPU Status',
'内存状态': 'Memory Status',
'磁盘状态': 'Disk Status',
'开发板温度': 'Board Temperature',
'4G网络状态': '4G Network Status',
'WiFi状态': 'WiFi Status',
'系统信息': 'System Information',
'性能监控': 'Performance Monitoring',
'网络状态': 'Network Status',
'系统日志': 'System Logs',
```

**新增EMS相关字段翻译:**
```javascript
'系统运行时长': 'System Uptime',
'当前系统时间': 'Current System Time',
'当前硬件时间': 'Current Hardware Time',
'CPU使用率历史': 'CPU Usage History',
'内存使用率历史': 'Memory Usage History',
'温度变化趋势': 'Temperature Trend',
'网络连接历史': 'Network Connection History',
'信号强度变化': 'Signal Strength Changes',
'系统运行日志': 'System Operation Log',
'重要事件记录': 'Important Events Record',
'该组件暂无可配置项': 'No configurable items for this component',
```

### 3. 添加语言切换时重新渲染对话框 (data.html 第8234-8238行)

```javascript
// 监听语言切换事件
window.addEventListener('languageChanged', function(e) {
    console.log('数据页面接收到语言切换事件:', e.detail.lang);

    // 更新HTML lang属性
    document.documentElement.lang = e.detail.lang === 'zh' ? 'zh-CN' : 'en';

    // 重新应用翻译到所有元素
    applyTouchscreenTranslations();

    // 如果字段设置对话框打开,重新渲染内容
    const settingsPanel = document.getElementById('settingsPanel');
    if (settingsPanel && settingsPanel.classList.contains('show')) {
        renderSettingsContent(currentComponent);
    }
});
```

## 修复效果

### ✅ 英文环境

**整机 - 实时数据标签页:**
| 章节标题 | 字段标签 |
|---------|---------|
| Strategy Parameters | Current Strategy, Dispatch Command, Target SOC, Max Power |
| Core Operation Parameters | SOC, SOH, Temperature, Charge/Discharge Power, Battery Current, Battery Voltage |
| Operation Statistics | Today's Charge, Today's Discharge, Total Charge, Total Discharge |
| Other Parameters | Cycle Count |

**整机 - 历史数据标签页:**
| 章节标题 | 字段标签 |
|---------|---------|
| Chart Analysis | Power & SOC Analysis, Revenue Analysis |

**EMS - 实时数据标签页:**
| 章节标题 | 字段标签 |
|---------|---------|
| CPU Status | Usage, Temperature |
| Memory Status | Occupancy |
| Disk Status | Disk Usage |
| Board Temperature | Current Temp |
| 4G Network Status | Signal Strength, SIM Status, Network Interface |
| WiFi Status | Signal Strength, Network Interface |
| System Information | System Uptime, Current System Time, Current Hardware Time |

**EMS - 历史数据标签页:**
| 章节标题 | 字段标签 |
|---------|---------|
| Performance Monitoring | CPU Usage History, Memory Usage History, Temperature Trend |
| Network Status | Network Connection History, Signal Strength Changes |
| System Logs | System Operation Log, Important Events Record |

### ✅ 中文环境

所有内容保持中文显示,无变化。

## 测试验证

### 场景1: 英文环境打开字段设置

**步骤:**
1. 清除 localStorage
   ```javascript
   localStorage.removeItem('touchscreen_lang');
   localStorage.removeItem('language');
   localStorage.removeItem('touchscreen_language');
   location.reload();
   ```
2. 打开数据页面(默认英文)
3. 点击"Overall" → "Real-time Data"
4. 点击右上角"Custom"按钮

**期望结果:**
- ✅ 对话框标题显示 "Field Settings"
- ✅ 标签页显示 "Real-time Data"、"Historical Data"
- ✅ 所有章节标题显示英文
- ✅ 所有字段标签显示英文

### 场景2: 切换历史数据标签页

**步骤:**
1. 在字段设置对话框中
2. 点击 "Historical Data" 标签

**期望结果:**
- ✅ 章节标题显示 "Chart Analysis"
- ✅ 字段标签显示 "Power & SOC Analysis"、"Revenue Analysis"

### 场景3: 切换到EMS设备

**步骤:**
1. 关闭对话框
2. 点击左侧 "EMS" 设备
3. 点击右上角 "Custom" 按钮

**期望结果:**
- ✅ 所有EMS相关章节标题显示英文
- ✅ 所有字段标签显示英文

### 场景4: 对话框打开时切换语言

**步骤:**
1. 在英文环境下打开字段设置对话框
2. 点击右上角语言切换按钮
3. 选择"中文"

**期望结果:**
- ✅ 对话框内容立即切换为中文
- ✅ 所有章节标题和字段标签都变为中文
- ✅ 切换流畅,无需重新打开对话框

### 场景5: 中文环境打开字段设置

**步骤:**
1. 在中文环境下
2. 点击"整机" → "实时数据"
3. 点击"自定义"按钮

**期望结果:**
- ✅ 对话框标题显示 "数据显示设置"
- ✅ 标签页显示 "实时数据"、"历史数据"
- ✅ 所有章节标题显示中文
- ✅ 所有字段标签显示中文

## 技术总结

### 动态内容国际化的关键点

1. **动态生成的DOM元素**: 不能使用 `data-i18n` 属性,必须在生成时调用翻译函数
2. **翻译时机**: 在设置 `textContent` 时立即翻译
3. **语言切换响应**: 监听 `languageChanged` 事件,重新渲染动态内容
4. **翻译完整性**: 确保所有动态生成的文本都经过 `translateLabel()` 处理

### 代码模式

**动态生成DOM元素时翻译:**
```javascript
const element = document.createElement('div');
element.textContent = translateLabel(sourceText);  // ✅ 正确
// element.textContent = sourceText;  // ❌ 错误
```

**语言切换时重新渲染:**
```javascript
window.addEventListener('languageChanged', function(e) {
    if (dialogIsOpen) {
        renderDialogContent();  // 重新渲染
    }
});
```

## 完整修复历史

这是数据页面国际化的第7轮修复:

1. ✅ localStorage 键名统一
2. ✅ 数据不显示问题修复
3. ✅ 交互功能失效修复
4. ✅ 静态HTML元素国际化
5. ✅ 动态内容国际化(设备状态、卡片值)
6. ✅ 历史数据图表完整国际化
7. ✅ **字段设置对话框国际化** ← 本次修复

## 相关文档

1. [重要-请先阅读.md](重要-请先阅读.md) - 总体概览
2. [数据页面完整国际化修复说明.md](数据页面完整国际化修复说明.md) - 静态HTML国际化
3. [数据页面动态内容国际化修复.md](数据页面动态内容国际化修复.md) - 卡片动态内容
4. [历史数据图表国际化修复完成.md](历史数据图表国际化修复完成.md) - 图表国际化
5. [完整测试清单-数据页面国际化.md](完整测试清单-数据页面国际化.md) - 详细测试步骤

## 🎉 修复完成!

字段设置对话框的所有内容(章节标题、字段标签、标签页按钮)都已完整支持中英文国际化,并且支持对话框打开时实时语言切换!

现在可以测试了! 🚀
