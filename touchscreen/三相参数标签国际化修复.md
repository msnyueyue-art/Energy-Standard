# 三相参数标签国际化修复

## 🐛 问题描述

在PCS和Meter(电表)页面的三相参数卡片中,"A相"、"B相"、"C相"的标签在英文环境下仍显示中文。

**影响页面:**
- PCS页面 - 电网侧参数
- Meter(电表)页面 - 电压和电流

## 🔍 问题分析

### 代码现状
`createThreePhaseCard`函数中已经正确使用了`translateLabel`:

```javascript
// data.html 第4045行
${translateLabel('A相')}

// data.html 第4065行
${translateLabel('B相')}

// data.html 第4084行
${translateLabel('C相')}
```

### 问题根因
翻译表中缺少"A相"、"B相"、"C相"的英文翻译,导致`translateLabel`函数返回原文本。

## 🔧 修复内容

### 翻译表补充 (data.html 第4295-4298行)

在`translateLabel`函数的翻译表中添加三相参数标签翻译:

```javascript
// 三相参数标签
'A相': 'Phase A',
'B相': 'Phase B',
'C相': 'Phase C',
```

## 📋 翻译对照表

| 中文 | 英文 |
|------|------|
| A相 | Phase A |
| B相 | Phase B |
| C相 | Phase C |

## 🧪 测试步骤

### 测试PCS页面

#### 步骤1: 清除缓存
**非常重要!** `Ctrl + F5`

#### 步骤2: 打开PCS页面
```
触摸屏系统 → Data → PCS → Real-time Data
```

#### 步骤3: 切换到英文
点击语言切换按钮 → 选择English

#### 步骤4: 验证三相标签

**电网侧参数 (pcsGridSideParams):**

**pcsGridVoltage卡片:**
- [ ] "A相" → "Phase A" ✓
- [ ] "B相" → "Phase B" ✓
- [ ] "C相" → "Phase C" ✓

**pcsGridCurrent卡片:**
- [ ] "A相" → "Phase A" ✓
- [ ] "B相" → "Phase B" ✓
- [ ] "C相" → "Phase C" ✓

**pcsGridPower卡片:**
- [ ] "A相" → "Phase A" ✓
- [ ] "B相" → "Phase B" ✓
- [ ] "C相" → "Phase C" ✓

### 测试Meter页面

#### 步骤1: 打开Meter页面
```
触摸屏系统 → Data → Meter → Real-time Data
```

#### 步骤2: 验证三相标签

**电压电流 (Voltage & Current):**

**Meter Voltage卡片:**
- [ ] "A相" → "Phase A" ✓
- [ ] "B相" → "Phase B" ✓
- [ ] "C相" → "Phase C" ✓

**Meter Current卡片:**
- [ ] "A相" → "Phase A" ✓
- [ ] "B相" → "Phase B" ✓
- [ ] "C相" → "Phase C" ✓

### 步骤3: 验证中文切换
切换回中文,验证:
- [ ] 所有三相标签正确显示"A相"、"B相"、"C相"
- [ ] 无英文残留

## 📁 修改文件

**touchscreen/data.html** - 共修改1处:
- 第4295-4298行: 添加三相参数标签翻译(3个条目)

**修改前:**
```javascript
                '6月': 'Jun',

                // 单位
                '次': 'Times',
```

**修改后:**
```javascript
                '6月': 'Jun',

                // 三相参数标签
                'A相': 'Phase A',
                'B相': 'Phase B',
                'C相': 'Phase C',

                // 单位
                '次': 'Times',
```

## 🔍 技术说明

### 三相卡片渲染流程
```
createThreePhaseCard(field, config)
  ↓
生成三相卡片HTML
  ↓
每个相使用 ${translateLabel('A相/B相/C相')}
  ↓
translateLabel 在翻译表中查找对应翻译
  ↓
返回英文 'Phase A/B/C' 或中文 'A相/B相/C相'
```

### 翻译机制
- **使用函数**: `translateLabel('A相')`
- **翻译表位置**: data.html内部的`labelTranslations`对象
- **语言判断**: 通过`getTouchscreenLang()`获取当前语言

### 影响范围
此修复会影响所有使用三相卡片的页面:
- ✅ PCS页面 - 电网侧参数(电压、电流、功率)
- ✅ Meter页面 - 电压电流

## ✅ 预期效果

修复后的三相参数卡片:
- ✅ 英文环境下显示 "Phase A", "Phase B", "Phase C"
- ✅ 中文环境下显示 "A相", "B相", "C相"
- ✅ 语言切换流畅,无残留
- ✅ 不影响其他页面功能

## ⚠️ 注意事项

### 浏览器缓存
**修改后必须清除缓存!**
1. 强制刷新: `Ctrl + F5`
2. 清除缓存: F12 → Application → Clear Storage
3. 无痕模式测试

### 相关页面
需要在以下页面测试:
1. **PCS页面**:
   - pcsGridVoltage
   - pcsGridCurrent
   - pcsGridPower

2. **Meter页面**:
   - Meter Voltage
   - Meter Current

### 显示位置
三相标签显示在卡片内部的每一行:
```
┌─────────────────────┐
│  pcsGridVoltage     │
├─────────────────────┤
│ Phase A    384.2 V  │  ← 这里
│ Phase B    383.6 V  │  ← 这里
│ Phase C    386.4 V  │  ← 这里
└─────────────────────┘
```

## 🐛 问题排查

如果修复后仍显示中文:

### 1. 检查浏览器缓存
- 必须强制刷新或清除缓存
- 尝试无痕模式

### 2. 检查控制台
- F12 → Console
- 查看是否有JavaScript错误

### 3. 检查语言设置
- F12 → Application → Local Storage
- `touchscreen_language` 应为 "en"

### 4. 验证文件修改
- 检查data.html第4295-4298行
- 确认包含三相标签翻译

### 5. 检查翻译函数
在控制台执行:
```javascript
translateLabel('A相')
// 应返回: "Phase A" (英文环境)
// 应返回: "A相" (中文环境)
```

## 📝 修复总结

本次修复解决了PCS和Meter页面三相参数标签的国际化遗漏问题:
- ✅ 识别问题: 翻译表缺失
- ✅ 添加翻译: 3个三相标签条目
- ✅ 影响范围: PCS和Meter的所有三相卡片
- ✅ 修复方式: 简洁高效,符合DRY原则

至此,触摸屏系统的三相参数标签已完全国际化!

---

**修复日期**: 2026-01-12
**修复内容**: 三相参数标签翻译
**影响页面**: PCS、Meter
**修复状态**: ✅ 完成

请清除缓存后测试!
