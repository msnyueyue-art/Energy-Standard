# 温度页面运行模式翻译修复

## 🐛 问题描述

在英文环境下,"运行模式"字段的值("制冷"/"制热")仍然显示中文,未被翻译成英文。

## 🔍 根本原因

运行模式字段有两个显示路径:
1. **初始渲染路径**: `createDataCard` → 使用 `getFieldValue` 获取值
2. **配置值路径**: 如果config中有预设value,直接使用config.value

问题在于这两个路径都没有对文本型的值进行翻译。

## 🔧 修复内容

### 1. 修复配置值路径的翻译 (第4369-4386行)

当字段有unit属性(即使unit为空字符串)时,添加值的翻译逻辑:

```javascript
} else if (config.unit) {
    // 有单位的数值
    const translatedUnit = translateUnit(config.unit);
    // 对于运行模式等文本型字段，需要翻译值
    const displayValue = (field === 'runningMode' || typeof config.value === 'string' && isNaN(config.value))
        ? translateLabel(config.value)
        : config.value;
    card.innerHTML = `
        <div class="card-title">
            ${showIcon ? `<span class="card-icon">${getFieldIcon(field)}</span>` : ''}
            ${translateLabel(config.label)}
        </div>
        <div class="card-value">
            <span id="${field}Value">${displayValue}</span>
            <span class="card-unit">${translatedUnit}</span>
        </div>
        ${config.status ? `<div style="font-size: 16px; color: ${statusColor || '#666'}; margin-top: 4px;">${translateLabel(config.status)}</div>` : ''}
    `;
}
```

### 2. 修复动态值路径的翻译 (第4422-4441行)

当使用`getFieldValue`获取值时,添加值的翻译逻辑:

```javascript
} else {
    // 普通字段显示数值和状态
    // 对于运行模式等文本型字段，需要翻译值
    const displayValue = (field === 'runningMode' || (typeof fieldValue === 'string' && isNaN(fieldValue) && !fieldValue.includes('.')))
        ? translateLabel(fieldValue)
        : fieldValue;
    card.innerHTML = `
        <div class="card-title">
            ${showIcon ? `<span class="card-icon">${getFieldIcon(field)}</span>` : ''}
            ${translateLabel(config.label)}
        </div>
        <div class="card-value">
            <span id="${field}Value">${displayValue}</span>
            ${config.unit ? `<span class="card-unit">${translateUnit(config.unit)}</span>` : ''}
        </div>
        ${statusText ? `<div class="card-status ${statusClass}">
            <span>${translateLabel(statusText)}</span>
        </div>` : ''}
    `;
}
```

### 3. 翻译逻辑说明

两处修复都使用了相同的逻辑来判断是否需要翻译:

```javascript
const displayValue = (field === 'runningMode' || (typeof value === 'string' && isNaN(value)))
    ? translateLabel(value)
    : value;
```

判断条件:
- `field === 'runningMode'`: 明确指定运行模式字段需要翻译
- `typeof value === 'string' && isNaN(value)`: 非数字的文本值需要翻译

### 4. 已有的动态更新翻译 (第5540-5545行)

数据动态更新时的翻译已正确实现:

```javascript
// 特殊处理运行模式字段
if (fieldName === 'runningMode') {
    const mode = Math.random() > 0.3 ? '制冷' : '制热';
    valueSpan.textContent = translateLabel(mode);
    return;
}
```

## 📋 翻译对照表

运行模式的翻译已在翻译表中定义(第4242-4243行):

```javascript
'制冷': 'Cooling',
'制热': 'Heating',
```

## 🧪 测试验证

### 测试步骤:
1. **清除缓存**: `Ctrl + F5` (必须!)
2. **打开温度页面**: Data → Temp → Real-time Data
3. **切换到英文**: 点击语言切换按钮
4. **验证运行模式**:
   - "制冷" → "Cooling" ✓
   - "制热" → "Heating" ✓

### 预期效果:
- ✅ 页面加载时,运行模式显示英文
- ✅ 语言切换时,运行模式正确切换
- ✅ 数据更新时,运行模式保持英文
- ✅ 刷新页面后,运行模式仍显示英文

## 📁 修改文件

**touchscreen/data.html** - 共修复3处:
1. 第4369-4386行: 修复配置值路径的翻译
2. 第4422-4441行: 修复动态值路径的翻译
3. 第5540-5545行: 已有的更新时翻译(无需修改)

## ✅ 修复状态

- ✅ 初始渲染时翻译
- ✅ 配置值路径翻译
- ✅ 动态值路径翻译
- ✅ 数据更新时翻译
- ✅ 语言切换时翻译

所有路径的翻译已完全修复,运行模式在英文环境下将正确显示"Cooling"或"Heating"。

## 🎉 修复完成

现在温度页面的运行模式字段已完全国际化,在英文环境下不会再显示中文。请清除缓存后测试!
