# 消息策略输入框国际化修复说明

## 问题描述
在英文环境下,消息策略管理页面([rule-engine.html](rule-engine.html))点击"新建策略"或"从模板创建"按钮后,弹框内的"策略名称"和"策略描述"输入框的占位符文本仍然显示中文,未能正确应用国际化翻译。

## 问题定位

### 受影响的位置
**文件**: [rule-engine.html](rule-engine.html:1499,1504)

**问题代码**:
```html
<!-- 策略名称输入框 - 已有翻译属性但需验证 -->
<input type="text" class="form-input" id="ruleName"
       data-translate-placeholder="ruleStrategyNamePlaceholder"
       placeholder="请输入策略名称" required>

<!-- 策略描述输入框 - 硬编码英文占位符 -->
<input type="text" class="form-input" id="ruleDescription"
       placeholder="Rule Description">
```

### 问题分析
1. "策略名称"输入框已有 `data-translate-placeholder` 属性,翻译键已存在
2. "策略描述"输入框的占位符是硬编码的英文 "Rule Description"
3. 缺少 `ruleStrategyDescPlaceholder` 翻译键
4. 缺少 `data-translate-placeholder` 属性

## 修复方案

### 1. 添加翻译键到 common.js

#### 中文翻译 (第2523行)
```javascript
ruleStrategyDescPlaceholder: '请输入策略描述 (可选)',
```

#### 英文翻译 (第6199行)
```javascript
ruleStrategyDescPlaceholder: 'Please enter strategy description (optional)',
```

### 2. 更新 rule-engine.html

**修改前** (第1504行):
```html
<input type="text" class="form-input" id="ruleDescription" placeholder="Rule Description">
```

**修改后** (第1504行):
```html
<input type="text" class="form-input" id="ruleDescription"
       data-translate-placeholder="ruleStrategyDescPlaceholder"
       placeholder="请输入策略描述 (可选)">
```

## 修改文件

### [common.js](common.js)
- **位置**: 第2523行 (中文翻译)
- **位置**: 第6199行 (英文翻译)
- **新增**: 1个翻译键,中英文各1个

### [rule-engine.html](rule-engine.html)
- **位置**: 第1504行 (策略描述输入框)
- **修改**: 添加 `data-translate-placeholder` 属性,修改默认占位符

## 修改统计

```
common.js        | +1 行 (中文翻译)
                 | +1 行 (英文翻译)
rule-engine.html | ~1 行 (添加翻译属性)
──────────────────────────────────────
2 files changed, 2 insertions(+), 1 deletion(-)
```

## 测试验证

### 测试步骤
1. 打开消息策略管理页面: [rule-engine.html](rule-engine.html)
2. 切换到英文语言模式
3. 点击"New Strategy"按钮或"Create from Template"
4. 在弹出的对话框中查看输入框占位符:
   - 策略名称: "Please enter strategy name"
   - 策略描述: "Please enter strategy description (optional)"
5. 切换回中文,验证显示:
   - 策略名称: "请输入策略名称"
   - 策略描述: "请输入策略描述 (可选)"

### 预期结果

#### 中文环境
```
策略名称 *
┌─────────────────────────────────────────┐
│ 请输入策略名称                          │
└─────────────────────────────────────────┘

策略描述
┌─────────────────────────────────────────┐
│ 请输入策略描述 (可选)                   │
└─────────────────────────────────────────┘
```

#### 英文环境
```
Strategy Name *
┌─────────────────────────────────────────┐
│ Please enter strategy name              │
└─────────────────────────────────────────┘

Strategy Description
┌─────────────────────────────────────────┐
│ Please enter strategy description (optional) │
└─────────────────────────────────────────┘
```

## 相关翻译键

现在新建策略弹框中的输入框已完整国际化:

| 翻译键 | 中文 | English |
|--------|------|---------|
| `ruleStrategyNameLabel` | 策略名称 | Strategy Name |
| `ruleStrategyNamePlaceholder` | 请输入策略名称 | Please enter strategy name |
| `ruleStrategyDescLabel` | 策略描述 | Strategy Description |
| `ruleStrategyDescPlaceholder` | 请输入策略描述 (可选) | Please enter strategy description (optional) |

## 设计原则遵循

### ✅ KISS (Keep It Simple, Stupid)
- 直接使用 `data-translate-placeholder` 属性
- 保持与其他输入框一致的翻译模式
- 简单清晰的占位符文本

### ✅ DRY (Don't Repeat Yourself)
- 统一使用国际化系统
- 复用现有的翻译机制
- 避免重复定义占位符文本

### ✅ 单一职责原则
- 翻译管理集中在 common.js
- 页面只负责标记需要翻译的元素
- 清晰的职责划分

## 修复完成时间
2026-01-14

## 总结

本次修复为消息策略管理页面的输入框添加了完整的国际化支持:

✅ **中文翻译**: 添加1个翻译键
✅ **英文翻译**: 添加1个翻译键
✅ **代码更新**: 添加 `data-translate-placeholder` 属性
✅ **完整覆盖**: 策略名称和策略描述输入框均已国际化

现在新建策略弹框的所有输入框占位符都能根据用户的语言设置自动切换中英文显示! 🎉
